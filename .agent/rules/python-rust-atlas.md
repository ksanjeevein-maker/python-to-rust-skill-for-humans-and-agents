# Python to Rust Atlas rule

Use the `python-rust-atlas` skill for:

- Python-to-Rust rewrites
- explaining Rust concepts to Python developers
- mapping Python collections, exceptions, classes, or async patterns into Rust
- citing official docs for Python, Rust, Tokio, Reqwest, or Serde

Read `.agent/skills/python-rust-atlas/SKILL.md` first, then pull only the
reference file needed for the task.
